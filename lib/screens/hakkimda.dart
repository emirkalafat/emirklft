import 'package:flutter/material.dart';

class HakkimdaPage extends StatelessWidget {
  const HakkimdaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Hakkımda")),
    );
  }
}
